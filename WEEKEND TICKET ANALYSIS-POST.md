For provided ticket numbers:

Go to SPC 
	then "Monitoring"
		then "Tickets"
		  and search details


 List whether or not was executed in the end

If not, what was the show stopper?
was anything required by the customer that was not provided?
Did the Customer request to schedule?
Did the processor missed the time window?
If not executed was there anything related to system failure (tool issue)?


Create a summary in a table form with columns TICKET, EXECUTED?, SHOW STOPPER, CUSTOMER MISSING ACTION, CUSTOMER RESCHEDULED, MISSED WINDOW BY PROCESSOR, SYSTEM TOOL FAILURE?
